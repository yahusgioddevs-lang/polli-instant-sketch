# Instant Polli Sketch
Tiny web app using Pollinations `flux` to turn a prompt into an image in one click.

## Deploy
```bash
export POLLINATIONS_API_KEY=sk_…
python app.py
```
Open http://localhost:8080
